{
  "name": "stub",
  "description": "",
  "version": "0.0.1",
  "author": {
    "name": "Josh Kraker",
    "email": "joshkrak@gmail.com"
  },
  "type": "commonjs",
  "main": "./dist/index.js",
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist/*",
    "prestart": "npm run build",
    "start": "node -r dotenv/config --unhandled-rejections=strict --trace-warnings .",
    "start-mon": "nodemon --config nodemon.json -r dotenv/config --unhandled-rejections=strict --trace-warnings src/index.ts",
    "build": "npm run clean && ttsc",
    "lint": "eslint ./src/**/*.ts"
  },
  "dependencies": {
    "@krakerxyz/typed-base": "file:../../typed-base",
    "dotenv": "^10.0.0",
    "fastify": "^3.19.1",
    "fastify-websocket": "^3.2.0",
    "netled": "file:../core"
  },
  "devDependencies": {
    "@types/node": "^14.17.5",
    "@typescript-eslint/eslint-plugin": "^4.22.0",
    "@typescript-eslint/parser": "^4.22.0",
    "eslint": "^7.24.0",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.0",
    "ts-node": "^10.1.0",
    "ttypescript": "^1.5.12",
    "typescript": "^4.2.4"
  }
}